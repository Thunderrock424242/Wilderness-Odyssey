# Public URL only. Never put an API token, GitHub token, password, or secret here.
PUBLIC_ADMIN_API_BASE=https://admin-api.example.com

# Local development only. Production builds ignore this switch by design.
# The normal real editor command (`npm run admin`) configures its local API automatically.
PUBLIC_ADMIN_MOCK=false
