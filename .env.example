# Non-secret canonical origin. Leave local unless preparing an approved deployment.
SITE_URL=http://localhost:4321

# Development-only content-editor mock. Ignored in production.
PUBLIC_ADMIN_MOCK=false

# Runtime connections and secrets belong in Pages environment bindings.
# See docs/cloudflare-deployment.md and .dev.vars.example.
