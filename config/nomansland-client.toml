[fog_modifiers]
	#If the custom fog modifiers are enabled
	fogModifiers = true
	#If the cave biome fog modifier is enabled
	caveBiomeFogModifier = true
	#If the deep dark fog modifier is enabled
	deepDarkFogModifier = true
	#If the foggy biome fog modifier is enabled
	foggyBiomeFogModifier = true
