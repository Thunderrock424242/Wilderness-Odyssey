{
  "name": "wilderness-odyssey-site",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-blog-share-pages.mjs && vite --configLoader native --host 127.0.0.1",
    "build": "tsc && node scripts/generate-blog-share-pages.mjs && vite build --configLoader native && node scripts/copy-social-assets.mjs",
    "preview": "vite preview --configLoader native --host 127.0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
