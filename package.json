{
  "name": "wilderness-odyssey-site",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:preview": "node scripts/generate-social-preview.mjs",
    "dev": "npm run generate:preview && vite --configLoader native --host 127.0.0.1",
    "build": "npm run generate:preview && tsc && vite build --configLoader native",
    "preview": "vite preview --configLoader native --host 127.0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
